datasource db {
  provider  = "postgresql"
  // 1. Usamos la URL acelerada para que la web vaya rápido
  url       = env("PRISMA_DATABASE_URL")
  // 2. Usamos la URL directa para cuando hagas migraciones (cambios de estructura)
  directUrl = env("POSTGRES_URL")
}